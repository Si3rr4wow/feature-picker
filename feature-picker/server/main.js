import { Meteor } from 'meteor/meteor';
import '../imports/api/tasks.js';
import '../imports/api/releaseNotes.js';
import '../imports/api/hearts.js';

Meteor.startup(() => {
  // code to run on server at startup
});
